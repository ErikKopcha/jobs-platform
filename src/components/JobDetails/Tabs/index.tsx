import React from 'react';
import { Text, View } from 'react-native';

const Tabs = (): React.ReactElement => {
  return (
    <View>
      <Text>Tabs</Text>
    </View>
  );
};

export default Tabs;
