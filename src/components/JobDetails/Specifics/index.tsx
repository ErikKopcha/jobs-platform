import React from 'react';
import { Text, View } from 'react-native';

const Specifics = (): React.ReactElement => {
  return (
    <View>
      <Text>Specifics</Text>
    </View>
  );
};

export default Specifics;
