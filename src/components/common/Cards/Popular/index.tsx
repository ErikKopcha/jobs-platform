import React from 'react';
import { Text, View } from 'react-native';

const PopularJobCard = (): React.ReactElement => {
  return (
    <View>
      <Text>PopularJobCard</Text>
    </View>
  );
};

export default PopularJobCard;
